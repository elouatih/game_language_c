#include<stdio.h>
#include<stdlib.h>
#include "game_aux.h"
#include "game.h"
int main(void){
    /*square *squares = malloc(DEFAULT_SIZE * DEFAULT_SIZE * sizeof(square));
    for(int i=0; i<DEFAULT_SIZE * DEFAULT_SIZE; i++){
        squares[i] = S_BLACK;
    }
    game default_game = game_default();
    game new_game = game_new(squares);
    game_print(new_game);
    game_print(default_game);*/
    game_default();
}
